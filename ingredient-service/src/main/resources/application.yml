server:
  port: 0 #0-app staring on a random port number 


spring: 
  application: 
    name: ingredient-service

#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka/

#Using config server
  cloud:
    config:
      uri: http://localhost:8888

---
spring:
  profiles: prod
    
#For production it's better
eureka:
  client:
    service-url:
      defaultZone: http://eureka1.tacocloud.com:8761/eureka/, http://eureka2.tacocloud.com:8762/eureka